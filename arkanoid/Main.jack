class Main {
	function void main() {
		var Game game;
		var boolean running;
		let game = Game.new();
		do game.start();
		let running = true;
		while (running) {
			let running = game.run();
		}
		return;
	}
}
